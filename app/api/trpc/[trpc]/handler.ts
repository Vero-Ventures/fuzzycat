import { NextResponse } from 'next/server';

// Placeholder until tRPC is configured (#11)
export async function GET() {
  return NextResponse.json({ status: 'tRPC not yet configured' });
}

export async function POST() {
  return NextResponse.json({ status: 'tRPC not yet configured' });
}
