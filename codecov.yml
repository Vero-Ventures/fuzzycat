coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 0.5%
    patch:
      default:
        target: 70%

comment:
  layout: "condensed_header, condensed_files, condensed_footer"
  require_changes: true

ignore:
  - "drizzle/migrations"
  - "scripts"
  - "e2e"
  - "**/*.config.ts"
  - "server/emails/**"
  - "components/ui/**"
